<!DOCTYPE html>
<html>
  <head>
    <title>Boggle</title>
    <%= csrf_meta_tags %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/foundation/6.2.4/foundation.min.css">
    <link href="https://fonts.googleapis.com/css?family=Raleway" rel="stylesheet">

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>

    <nav>
      <div class="nav row">
        <div class="logo small-3 columns">Boggle</div>
        <% if user_signed_in? %>
          <div class="nav-link small-9 columns"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></div>
        <% else %>
          <div class="nav-link small-3 columns"><%= link_to 'Sign In', new_user_session_path %></div>
          <div class="nav-link small-6 columns"><%= link_to 'Sign Up', new_user_registration_path %></div>
        <% end %>
      </div>
    </nav>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
